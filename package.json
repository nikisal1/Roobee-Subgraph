{
  "name": "roobee-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ nikisal1/roobee-subgraph",
    "create-local": "graph create --node http://localhost:8020/ nikisal1/roobee-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ nikisal1/roobee-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nikisal1/roobee-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
